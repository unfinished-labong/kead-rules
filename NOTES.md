# kead-rules 작업 노트

한국장애인고용공단 규정 검색 MCP 서버. 이 문서는 **배경 지식이 없는 사람(또는 AI)이
바로 이어서 작업할 수 있게** 쓴 인계 메모다. 코드를 읽으면 알 수 있는 것은 줄이고,
**코드에 남지 않는 것**(왜 그렇게 했는지, 무엇을 시도했다 버렸는지)을 위주로 적었다.

---

## 1. 이 시스템이 하는 일

공단 직원이 Claude 같은 AI에게 "큰아버지가 돌아가시면 휴가가 나오나?" 하고 물으면,
AI가 이 서버를 통해 **실제 규정 조문을 찾아서** 근거를 대고 답하게 하는 것.

핵심 요구는 두 가지다.

- **환각 금지.** 조문을 지어내면 안 된다. 근거가 없으면 없다고 해야 한다.
- **누락 금지.** 답이 여러 조문에 흩어져 있으면 다 찾아야 한다.
  실제로 담당자가 "실시상황보고가 빠졌다"고 지적해야 겨우 찾아내는 일이 반복됐다.

### 자료 흐름

```
공단 홈페이지 게시판 ─┐
                     ├→ collect.js ─→ fetch-docs.js ─┐
법제처 OPEN API ──────┘                (kordoc 변환)   ├→ build-index.js ─→ data/index.json
                       fetch-law.js ──────────────────┘                          │
                                                                                  ↓
                                                              fly.io 의 server.js 가 읽어서 검색
```

- **fetch-docs.js** — 공단 홈페이지 첨부(hwp/hwpx)를 받아 `kordoc` 으로 조문을 뽑는다. 74건.
- **fetch-law.js** — 법제처 OPEN API 로 법률·시행령·고시를 받는다. 44건. `LAW_OC` 키 필요.
- **build-index.js** — 둘을 합쳐 `data/index.json` 을 만든다. 문서 118건 / 조문 약 5,900건.
- **server.js** — 인덱스를 메모리에 올리고 MCP 도구를 제공한다. 외부 라이브러리 없음.
- 매일 새벽 5시(KST) GitHub Actions 가 위를 전부 돌리고 `data/index.json` 을 커밋한다.
- 서버는 `REFRESH_MIN`(기본 30분)마다 GitHub raw 에서 인덱스를 다시 받는다.

### 도구 4개

| 도구 | 하는 일 |
|---|---|
| `search_provisions` | 낱말로 조문을 찾는다. 2단계로 돈다(아래 3-1 참고) |
| `get_provision` | 문서명+조문번호로 원문을 그대로 가져온다. `목차`, `전문` 도 됨 |
| `find_cycle_duties` | 주기가 정해진 업무를 전수로 훑어 모은다(아래 3-3) |
| `list_documents` | 수록 문서 목록 |

### 검색 원리 (요약)

한국어는 조사가 붙어 낱말 경계가 흐리므로 **2-gram(글자 두 개씩)** 으로 색인한다.
조각마다 IDF(희소성)를 매기고, 흔한 조각보다 드문 조각을 무겁게 친다.
그 위에 도메인 가중치가 얹혀 있다 — 별표 0.75 / 부칙 0.45, 문서명 일치 보너스,
장·절 제목 일치 보너스, 길이 정규화, 조문번호 지목 시 3배.

**이 IDF 구조가 설계의 뼈대다.** 뒤에 나오는 "라이브러리를 안 쓰는 이유"가 여기서 나온다.

---

## 2. 현재 상태

```
node eval.js --save base.json      # 평가 실행 (Actions → '검색 품질 평가' 로도 가능)

hit@1 10/14 · hit@3 11/14 · MRR 0.759
종합형 3/4건 통과 · 필요 조문 16/20개 담김
되묻기 1건 · 약한결과 1건 · 무관질의 방어 1/1
```

- 문서 118건 / 조문 약 5,900건 / 별표 약 1,030건
- 별표 내역: 법제처 301건(2026-08-03 추가) + 내부규정 877건(같은 날 +144건 수정)

---

## 3. 적용된 것 — 무엇을, 왜

### 3-1. 2단계 검색 (server.js `search_provisions`)

**문제.** 조문을 바로 고르면 엉뚱한 규정의 조문이 뒤섞였다. 또 "휴가"처럼 흔한 낱말은
어느 규정에나 있어서 변별력이 없었다.

**해법.** 조문이 아니라 **문서를 먼저 정한다.**

```
1단계  전체를 훑어 조문 60건을 받는다
2단계  조문 점수를 문서별로 합산(top1 + 0.3×top2 + 0.15×top3)해 성격마다 1등을 고른다
       성격 = 법률/대통령령/부령/고시/훈령/규정/규칙/지침/정관 (내부규정은 이름 끝으로 판별)
3단계  정해진 문서 안에서만 다시 찾는다. 범위가 좁으므로 문턱을 8점 → 2.4점으로 낮춘다
4단계  좁히다 놓치면 1단계 결과로 되돌아간다(후퇴 경로 필수)
```

3단계의 문턱 완화가 핵심이다. 원래 `document` 를 지정하면 문턱이 0.3배가 되는 장치가
있었는데, **문서 이름을 이미 아는 사람만 쓸 수 있었다.** 그걸 자동으로 돌리는 것이다.

**주의할 점 두 가지.**
- 문서를 정할 때 **별표는 세지 않는다.** 별표는 부속 자료라 그 문서가 무엇에 관한 것인지
  말해주지 않는다. 별표를 세면 엉뚱한 규정이 1등이 된다(실측으로 확인).
- 조문번호(`제25조`)가 질의에 있으면 2단계를 건너뛴다. 답이 이미 정해진 질문이다.

### 3-2. 되묻기

성격 안에서 1등이 2등의 2배에 못 미치면 사용자에게 묻는다. 단, **네 조건을 모두** 만족할 때만:

- 전체 1등이 걸린 성격일 것 (엉뚱한 성격의 접전으로 답 전체를 막지 않는다)
- 3개 이상이 엉켜 있을 것 (둘뿐이면 묻지 말고 둘 다 열어둔다 — 조문 점수가 알아서 가린다)
- 1등 점수가 `MIN_SCORE * 3` 이상일 것 (약한 후보끼리의 접전은 물어도 답이 안 나온다)
- 형제 문서가 아닐 것 (규정↔규칙은 형제 안내가 따로 챙긴다)

조건을 좁히는 과정에서 되묻기 비율이 12건 중 3건 → 1건으로 줄었다.

### 3-3. `find_cycle_duties` — 주기 업무 전용 도구

**문제.** "분기별로 뭘 챙겨야 하나" 같은 질문에서 실시상황보고(지원규정 제16조)를 계속 놓쳤다.
이유가 명확하다 — 제16조 본문에 `매분기`는 **딱 한 번 스칠 뿐**이고, 그 조문의 주제어는
`훈련실시상황보고`인데 질문에는 그 낱말이 없다. 색인이 아무리 좋아도 안 걸리는 구조다.

**해법.** 검색이 아니라 **전수 훑기.** 조문을 처음부터 끝까지 보면서
`매월/매분기/매반기/매년/연 N회/수시` 표현이 있는 것만 골라 주기별로 묶어 돌려준다.

이 아이디어는 **담당자가 실제로 하는 방식**에서 나왔다("나는 반기별·분기별을 검색해본다").

**추가로: 모델이 이 도구를 고르길 기다리지 않는다.** 질문에 `주기·정기·분기·반기·챙길·확인해야`
같은 말이 있으면 `search_provisions` 결과에 주기 업무 목록을 **자동으로 붙인다**.
도구를 만들어두고도 Haiku 가 안 써서 실패한 적이 있어서 선택권을 뺐다.

### 3-4. 나열 강제 (종합형 질문 대책)

**문제.** "주기적으로 확인할 업무" 질문에 지도점검만 500단어로 파고들고 나머지를 빠뜨렸다.
토큰을 아끼는 게 아니라 **한 항목을 붙들고 늘어지는** 실패다.

**해법.** 도구 출력에 직접 지시를 심는다.

```
이것은 목록형 질문입니다. 아래 N건을 하나도 빠뜨리지 말고 주기별로 한 줄씩 나열하십시오.
한 줄은 "무엇을 · 언제까지 · 근거조문" 이면 충분합니다. 조문 하나를 골라 깊게 풀어쓰지 마십시오.
나열을 마친 뒤 "어느 항목을 자세히 볼까요?" 라고 되물으십시오.
```

함께 **발췌를 220자 → 90자로 줄였다.** 긴 발췌는 "이걸로 문단을 쓰라"는 신호가 된다.

### 3-5. 형제 문서 안내

같은 사안이 「○○규정」과 「○○ 시행규칙」에 나뉘어 있는 일이 흔하다.
꼬리말(`규정`/`시행규칙`/`업무처리규칙`/`세칙`…)을 반복해서 떼어 줄기를 뽑고, 줄기가 같으면 한 집안으로 본다.
안 본 형제가 있으면 결과 아래에 알린다.

- **줄기 최소 길이는 2다.** 3으로 올리면 「복무규정」→「복무」가 걸러져서
  복무·인사·여비 같은 흔한 규정이 전부 집안을 못 이룬다(실측으로 확인).
- 조문을 지목했거나 결과가 2건 이하면 안내를 안 붙인다. 단일 질문에 붙이면 답이 흐려진다.

### 3-6. `[NOT_FOUND]` 문구 — "없다"고 단정하지 않기

**문제.** 도구가 "못 찾았다"와 "없다"를 구분하지 않고 모델에게 "없다고 답하라"고 지시했다.
실제로 조문이 있는데 "규정에 없습니다"라고 답한 사고가 났다.

**해법.**
- 문턱을 넘는 조문이 하나도 없으면 **빈손으로 돌려보내지 않는다.**
  점수가 낮은 후보 5건을 `[약한 결과]`로 밝히고 그대로 내보낸다.
- 문구에서 "없다"를 빼고, 일상어 → 규정 용어 대응표를 넣었다.
  (큰아버지·삼촌 → 친족/형제자매/경조사, 월급 → 보수, 출장비 → 여비 …)

### 3-7. 날짜 표기

두 날짜를 **조문마다** 붙인다. 답변 끝의 참고 문서를 모델이 빠뜨려도 눈에 걸리게.

```
  개정일: 2025-07-03 · 제255호  |  이 자료 수집일: 2026-08-03
```

- `dateKind` 는 문서마다 다르다(시행 53 / 개정 60 / 제정 5). 뭉뚱그리지 말고 원표기를 살린다.
- 참고 문서 목록은 **마크다운 링크**로 준다: `· [복무규정](https://…/d/18l7) (개정 …)`.
  주소를 따로 적으면 눌리지 않는다. 하이퍼링크는 강제할 수 없고, 이게 확률을 높이는 최선이다.
- 시행예정 문서가 섞이면 경고를 붙인다.

**주의: 문서명 자체에 날짜를 넣으면 안 된다.** 형제 문서 탐지가 죽는다.
꼬리말을 떼어 줄기를 뽑는 방식이라, 뒤에 날짜가 붙으면 뗄 게 없어진다.

### 3-8. HTML 뷰어 (`/doc/<이름>`, `/d/<짧은키>`)

**문제.** 공단 게시판 링크는 한글 파일을 내려받게 하고, 법제처 한글주소는 조문으로 못 간다.
게다가 한글주소가 반만 인코딩돼 있어 모델이 옮겨 적다 깨뜨렸다("페이지를 찾을 수 없습니다").

**해법.** 인덱스에 이미 전문과 표가 다 있으므로 **서버가 HTML 로 그려서 내보낸다.**
변환도 저장도 필요 없고, 인덱스가 갱신되면 자동으로 최신이다.

- 조문마다 `id="제25조"`, 별표마다 `id="별표2"` — 앵커로 정확히 그 자리에서 열린다
- 목차를 장·절로 묶고, 넓은 화면에서는 2단으로 흘린다
- 표는 이미 HTML(`<table>`)로 저장돼 있어 그대로 그려진다
- **짧은 키가 따로 있는 이유:** 한글 이름을 주소에 쓰면 링크 하나가 중앙값 161자, 최대 338자다.
  열 군데 붙이면 1,600자를 먹는다. `/d/2pan` 은 37자다(83% 절약).
  `/doc/복무규정` 도 그대로 열리므로 사람이 손으로 쳐도 된다.

### 3-9. 법제처 별표 수집 (2026-08-03)

**문제.** 법제처 문서 44건의 별표가 **전부 0건**이었다. 「장애인 직업능력개발훈련 지원규정」의
별표 2(출석인정일수·휴가 기준)를 못 찾아 AI 가 "원문을 직접 확인하셔야 합니다"라고 답했다.

**원인 1.** `parseArticles` 가 `조문` 노드만 읽고 `별표단위` 를 아예 안 봤다.
이미 받아오던 응답 안에 별표가 들어 있는데 버리고 있었다.

**원인 2 (더 중요).** XML 뿌리를 잘못 집었다.

```js
const root = doc['법령'] ?? doc['행정규칙'] ?? Object.values(doc)[0];
```

고시(행정규칙) 응답의 최상위 태그는 `행정규칙` 이 아니다. 그래서 세 번째로 넘어가는데,
그게 하필 **`<?xml version="1.0"?>` 선언**을 집는다. 태그는 다 멀쩡히 있는데 엉뚱한 데서 찾고 있었다.
`docRoot()` 로 선언·속성 마디를 건너뛰게 고쳤다.

**부수 효과가 컸다.** 같은 뿌리 찾기를 `parseBody` 도 쓰고 있어서, 고시의 **조문 파싱까지 함께 고쳐졌다.**

| 문서 | 전 | 후 |
|---|---|---|
| 장애인 직업능력개발훈련 지원규정 | 조문 1 | 조문 50 · 별표 15건 |
| 사업주 및 장애인 등에 대한 융자·지원규정 | 조문 1 | 조문 84 · 별표 21건 |
| 장애인 취업지원 업무처리 규정 | 조문 1 | 조문 25 · 별표 10건 |

별표 응답에 본문이 있으면 그대로 쓰고, 없으면 `별표서식PDF파일링크` 로 파일을 받아
kordoc 으로 읽는다. PDF 를 먼저 시도한다(한글 파일보다 변환이 안정적).
본문을 못 받아도 **제목과 원문 링크는 남긴다** — "별표가 없다"보다 "별표는 있고 원문은 여기"가 낫다.

담는 그릇 이름(`별표`/`별표목록`/`별표단위`)은 대상마다 다르므로, **그릇이 아니라 알맹이로 찾는다**
(`별표번호`·`별표제목`·`별표서식파일링크` 같은 필드를 가진 마디).

### 3-10. 내부규정 별표 수집 버그 (2026-08-03)

법제처를 고친 뒤에도 「장애인 직업능력개발훈련 지원 업무처리규칙」의 별표가 안 나왔다.
**이건 별개의 버그였다.**

별표 제목이 이렇게 생긴 문서가 있다.

```
■ 장애인 직업능력개발훈련 지원 업무처리규칙 [별표 1] 삭제[2019.1.9.]
```

`isAnnexHeading` 이 줄 첫머리에서만 `[별표` 를 찾는데 앞에 `■ 문서명` 이 붙어 있어 못 봤다.
그 결과 별표가 통째로 「부칙」에 묻히고 조각까지 나서 인덱스에서 사라졌다.
**74건 중 16건이 같은 이유로 별표를 잃고 있었다.**

**고치다가 더 큰 사고를 낼 뻔했다.** 처음엔 "아무 줄에서나 `[별표` 앞을 잘라내자"로 했는데:

```
회계규정 시행규칙 · 별표 81→114 · 본칙 194→19    ← 본칙이 날아감
```

*"② 지원한도는 [별표 3] 참조"* 같은 **본문 줄까지 별표 제목으로 오인**해서, 그 뒤 조문이
전부 별표 구간에 삼켜졌다. `■` 같은 글머리 기호로 시작하는 줄에서만 앞자락을 걷어내도록 좁혔다.

결과: 늘어남 28건 · 그대로 46건 · **줄어듦 0건** / 별표 733 → 877건.

**캐시 판번호를 반드시 올려야 한다.** `fetch-docs.js` 의 `CONVERTER_VERSION` (지금 6).
첨부 파일이 안 바뀌면 예전 변환 결과를 그대로 쓰므로, 번호를 안 올리면 코드를 고쳐도 반영이 안 된다.

### 3-11. 괘선 표 복원

법제처 별표는 `┏━┓┃│` 괘선으로 그린 표인데, 수집 과정에서 **줄바꿈이 전부 사라진다**
(괘선 문자 74개인데 개행 0개). 그래서 뷰어에서 글자 뭉치로 보였다.

줄 끝이 `┓ ┨ ┛` 또는 **닫는 `┃`**(뒤에 `┃ ┠ ┗` 가 오는 것)이라는 규칙으로 되살리고,
고정폭 글꼴(`<pre>`)로 그리면 원본 표가 그대로 재현된다. PDF·이미지보다 낫다 —
글자를 복사할 수 있고, 검색되고, 용량도 안 든다. 원본 파일 링크는 별표 머리에 따로 달았다.

### 3-12. 기타

- **장·절 정보(`chapter`).** `docs/*.json` 의 `breadcrumb` 를 인덱스에 실었다.
  단, 그 값에는 문서명·앞 조문 본문이 섞여 들어온다(431건). `제N장/절/관/편` 으로 시작하는 것만 남긴다.
  검색 순위에는 영향이 없었지만, **장·절 제목이 질의와 맞으면 밀어주는 보너스(계수 2.5)** 는 효과가 컸다.
  계수를 0.8→1.5→2.5→4.0 으로 훑어 2.5 가 균형점이었다(4.0 은 hit@8 이 떨어진다).
- **본문 길이 상한을 구분별로 나눴다.** `{ 본칙: 6000, 별표: 12000, 부칙: 12000 }`.
  예전엔 1,800자였고 별표 227건이 잘려 나갔다. 색인 자체가 12,000자에서 잘리므로 그만큼 열어도 손해가 없다.
- **별표 조회 시 대괄호를 무시한다.** 실제 값은 `[별표 2] (제14조 관련) <개정 …>` 인데
  사람도 모델도 `별표 2` 라고 친다. 대괄호·공백을 지우고 견주고, 앞부분만 맞아도 받는다.
- **`dataAsOf` 는 한국 날짜로 적는다.** `toISOString()` 은 UTC 라서, 워크플로가 한국시간 새벽 5시
  (=UTC 20시)에 돌면 날짜가 하루 전으로 찍혔다. 커밋 메시지는 `date -u -d '+9 hours'` 로 맞춰져
  있었는데 `build-index.js` 만 안 맞아서 서로 달랐다.
- **`/health` 는 인덱스 적재 전 503 을 준다.** 200 을 주면 프록시가 준비된 줄 알고
  배포 도중 요청을 흘려보낸다.

---

## 4. 검토했다가 **버린 것** — 다시 제안하지 말 것

> 이 절이 이 문서에서 가장 값어치 있다. 코드 어디에도 안 남는 정보다.

### 4-1. FlexSearch + es-hangul + N-gram

- **FlexSearch 는 IDF 를 안 쓴다.** 점수가 문서 거리·부분 거리 기반이다.
  지금 설계의 뼈대가 IDF(희귀어가 신호)인데 그게 통째로 사라진다.
- **es-hangul 은 검색 라이브러리가 아니다.** 초성화와 조사 **붙이기**를 한다.
  검색에 필요한 건 반대 방향(조사 떼기)인데 그 기능이 없다.
- **N-gram 은 이미 쓰고 있다.** `grams()` 4줄이 그것이다.
- **도메인 로직은 어느 라이브러리도 안 준다.** 별표/부칙 가중치, 문서명 부스트, 조문 고정,
  `[NOT_FOUND]` 환각 방지 — 코드의 절반이고 전부 다시 얹어야 한다.

**유일하게 검토할 값어치가 있던 것은 MiniSearch**(BM25+, 토크나이저 교체 가능).
지금 코드에 TF(등장 횟수)가 없다는 빈 구멍을 채워줄 수 있다. 다만 법령 조문은 짧고
같은 낱말을 반복하지 않는 문체라 이득이 작을 것으로 봤다.
**조문 평균 길이가 800자를 넘고 실패가 "찾긴 찾는데 순위가 밀림"에 몰려 있을 때만** 다시 볼 것.

### 4-2. Gemini File Search 로 통째 교체

- File Search 는 `generateContent` 안에서 도는 기능이라 **서버가 없어지지 않는다.**
  Claude → 서버 → Gemini → File Search 가 되어 모델이 둘로 늘고, 요약 단계가 하나 더 낀다.
- 청킹을 Google 이 하므로 **조문 단위 무결성**이 깨진다. 인용 근거가 반쪽이 될 수 있다.
- `복무규정 제25조` 같은 조문 지목은 벡터 검색이 가장 약한 유형이다.
- 공단 내부규정을 외부 클라우드에 올리는 문제는 **기술이 아니라 결재 문제**다.

**결론: 교체가 아니라 `search_semantic` 도구를 하나 더 붙이는 방향.** 아직 안 함.

### 4-3. 개념 분할 패치 (되돌림)

질의를 공백으로 쪼개 경계 조각(`사휴`, `가휴` 같은 쓰레기)을 없애는 패치를 만들었다가
**검증 없이 배포해서 되던 것이 안 되게 만들었다.** 되돌렸다.
(그때 `cov = Math.max(...)` 를 제안했는데 이건 명백히 틀렸다 — 개념 하나만 맞아도
coverage 가 1.0 이 되어 필터가 무력화된다.)

**교훈: 검색 점수 계산과 도구 동작을 한 번에 바꾸지 말 것.** 어느 쪽이 범인인지 못 가린다.

### 4-4. fly.io 수면 모드 (`min_machines_running = 0`)

깨어날 때마다 GitHub raw 에서 5.3MB 를 받고(300~900ms) 역색인을 만든다(~600ms).
머신 부팅까지 합치면 첫 응답이 2~3초. 규정 검색은 업무 중에 툭 던지고 바로 받는 성격이라
그 지연이 체감으로 거슬린다. **아낄 돈이 몇 달러 수준이라 안 하기로 했다.**

참고: 헬스체크는 자동 정지를 막지 않는다(라우팅에만 관여). 걱정할 필요 없었던 부분.

### 4-5. 문서명에 날짜 넣기

`복무규정(개정 2025. 07. 03. · 제255호)` 처럼 날짜를 이름에 박아 강제하려 했으나,
**형제 문서 탐지가 죽는다.** 3-7 참고. 대신 조문 머리말에 넣었다.

### 4-6. 별표를 PDF·이미지로 저장

괘선 표가 깨져 보이길래 PDF 변환을 검토했으나, 원인이 폰트가 아니라 **줄바꿈 소실**이었다.
복원해서 고정폭으로 그리면 그만이다(3-11). HWP→PDF 변환은 LibreOffice + H2Orestart 로
가능하지만 구형 hwp 19건이 실패할 수 있고, 저장·서빙 부담이 크다.

---

## 5. 평가 체계 (`eval.js` + `eval-queries.json`)

```bash
node eval.js --save base.json                              # 기준선 저장
node eval.js --base base.json                              # 증감 표시
node eval.js --index data/index-new.json --base base.json  # 인덱스 바꿔 비교
node eval.js --server server-v7.js --base base.json        # 서버 바꿔 비교
node eval.js --only 강사료                                  # 한 건만
```

- **서버를 실제로 띄워 MCP 로 두드린다.** 검색 함수를 흉내 내지 않으므로 2단계 검색·되묻기·
  약한 결과가 그대로 점수에 반영된다.
- 인덱스와 서버를 따로 갈아끼울 수 있어서, 무엇을 바꿔도 평가표를 그대로 쓴다.
- **단일형**은 순위(hit@1/3/8, MRR), **종합형**(`type: "종합"`)은 필요한 조문을 몇 개나
  담았는지로 잰다. 절차·체크리스트는 답이 흩어져 있어 하나만 맞히고 통과시키면 쓸모를 못 잰다.
- GitHub Actions 에 `eval.yml` 이 있어 브라우저에서 버튼으로 돌릴 수 있다(로컬 환경 불필요).

### 이 평가기에서 겪은 함정

- **모델의 최종 답변으로 검색 엔진을 평가하면 안 된다.** 엔진은 제11조를 돌려줬는데 모델이
  버린 사례가 있었다. 이걸 엔진 실패로 집계하면 엉뚱한 곳을 고치게 된다.
  층을 나눠 재라 — **엔진 층**(도구 출력에 정답이 들어왔나) / **합성 층**(답변에 살렸나).
- **참고 문서 목록을 검색 결과로 세는 버그가 있었다.** `제772호` 를 조문번호로 오인.
  `(개정 …` `(시행 …` 이 붙은 줄을 걸러내도록 고쳤다. 점수가 부풀려질 수 있으니 주의.

### 정답지에 대한 경고

**`confirmed: false` 인 항목은 AI 가 인덱스를 뒤져 추정한 값이고, 자주 틀린다.**
이 대화에서만 세 번 틀렸다.

| 질의 | AI 가 적은 정답 | 실제 |
|---|---|---|
| 금품 수수 징계양정 기준 | 행동강령 [별표 1] | 인사규정 시행규칙 제85조 (둘 다 근거) |
| 근로지원인 신청 절차 | 제4조 하나 | 제3·4·7·10조에 걸쳐 있음 |
| 징계 절차 | 인사규정 제51조 | 인사규정 시행규칙 제79~83조 |

세 번 다 **검색이 맞았고 정답지가 틀렸다.** 실무 담당자 확인이 우선이다.

---

## 6. 일하는 방식 (지키면 시간을 아낀다)

1. **한 번에 하나씩만 바꾼다.** 검색 로직과 도구 동작을 같이 고치면 원인을 못 가린다.
2. **바꾸기 전후로 `eval.js` 를 돌린다.** 숫자가 안 움직이면 그 변경은 검색이 아니라
   표시를 고친 것이다(그것도 값어치가 있지만, 구분해서 말해야 한다).
3. **계수는 훑어서 정한다.** 감으로 고르지 말고 0.8/1.5/2.5/4.0 처럼 몇 단계 재보고 고른다.
   "이건 원인이 아니다"를 밝히는 것도 성과다(별표 가중치 0.75~1.15 를 훑어 배제한 적 있음).
4. **`DEBUG_SEARCH=1`** 로 띄우면 조문별 점수·coverage·문턱이 찍힌다. 문턱을 맞출 때 쓴다.
5. **로컬에서 서버를 띄워 curl 로 두드리면** 모델을 거치지 않고 도구 출력만 볼 수 있다.

```bash
INDEX_URL="http://127.0.0.1:9/none" INDEX_CACHE="$PWD/data/index.json" \
  PORT=8899 DEBUG_SEARCH=1 node server.js

curl -s -X POST localhost:8899/mcp -H 'Content-Type: application/json' \
 -d '{"jsonrpc":"2.0","id":1,"method":"tools/call",
      "params":{"name":"search_provisions","arguments":{"query":"큰아버지 사망 휴가"}}}'
```

`INDEX_URL` 을 죽은 주소로 주면 로컬 캐시로 뜬다.

6. **진단기를 만들 때 원본 코드를 다시 구현하지 말 것.** 진단기 자체의 버그와 진짜 문제를
   구별할 수 없게 된다. `probe-annex.js` 를 그렇게 만들었다가 한 번 헛돌았다.
   지금은 `fetch-law.js` 의 함수를 `import` 해서 쓴다.

---

## 7. 남은 것 / 알려진 문제

### 검색

- **`강사료 지급`** — 되묻기가 뜨고, 후보 순서가 나쁘다(정답으로 보이는 「교육훈련규칙」이 4순위).
  다만 AI 가 되묻기를 받아 교육훈련규칙으로 넘어가 답을 내는 것은 확인됨.
- **`표준사업장 사후관리`** — 4위 → 8위로 밀렸다. 고시가 13조문으로 쪼개지며 경쟁이 늘어난 탓.
- **어휘 격차는 여전하다.** `큰아버지` ↔ `형제자매` 는 글자가 하나도 안 겹친다.
  `SYNONYMS` 를 33개로 늘려 경조사 쪽은 막았지만, **손으로 관리하는 표는 영원히 불완전하다.**
  근본 해법은 임베딩 하이브리드(4-2 참고).
- **한 글자 낱말은 구조적으로 못 찾는다.** 색인이 2-gram 이라 `상` 같은 것은 조각을 못 만든다.

### 수집

- **법제처 API 가 간헐적으로 막힌다.** `UND_ERR_CONNECT_TIMEOUT` 으로 44건 전건 실패한 날이
  이틀 있었다(8/1, 8/2). 44건을 순차로 시도하며 각각 타임아웃을 기다려 35분이 걸린다.
  주 2회 이상 반복되면 재시도 간격과 조기 포기 로직을 넣을 것.
- **`fetch-law.js` 의 링크가 반만 인코딩돼 있다.** `https://www.law.go.kr/행정규칙/%EC%9E%A5...`
  — 앞은 날 한글, 뒤는 퍼센트 인코딩. 모델이 옮겨 적다 깨진다. 일련번호 기반 ASCII 주소로
  바꾸는 게 안전하다. (HTML 뷰어가 있어 급하진 않다.)
- **`직장 내 장애인 인식개선 교육 규정`, `장애인 기능경기 대회 등 운영에 관한 규정`** 은
  별표 0건이다. 원래 없는 것인지 확인 안 됨.

### 자료

- **연혁 123건을 안 쓰고 있다.** `inventory.json` 에 과거 개정본이 수집돼 있는데
  현행 72건만 인덱스에 들어간다. "이 조항 언제 바뀌었나" 에 답할 재료가 버려지는 중.
- **`eval-queries.json` 의 `confirmed: false` 항목** 실무 검증 필요.

---

## 8. 파일 목록

| 파일 | 역할 |
|---|---|
| `server.js` | MCP 서버 + HTML 뷰어. fly.io 에 배포됨 |
| `build-index.js` | `data/index.json` 생성 |
| `fetch-docs.js` | 공단 홈페이지 첨부 → kordoc 변환. `CONVERTER_VERSION` 주의 |
| `fetch-law.js` | 법제처 API 수집. `LAW_OC` 필요 |
| `collect.js` | 홈페이지 게시판 목록 수집 |
| `eval.js` | 검색 품질 평가기 |
| `eval-queries.json` | 평가 질의 + 정답 |
| `probe-annex.js` | 법제처 별표 응답 구조 진단기 |
| `.github/workflows/collect.yml` | 매일 새벽 5시(KST) 수집 |
| `.github/workflows/eval.yml` | 검색 품질 평가 (수동/푸시) |
| `.github/workflows/probe.yml` | 별표 진단 (수동) |

배포는 `Dockerfile` 이 `server.js` 만 COPY 한다. 나머지는 이미지에 안 들어간다.

---

*최종 갱신: 2026-08-03*
